#include<stdio.h>
#include<conio.h>
void main()
{
int ar[100],s,i,v,f=0;
clrscr();
printf("\n \n \t \t Search");
printf("\n Enter the size of the array");
scanf("%d",&s);
printf("\n input the elements in the firstt array");
for(i=0;i<s;i++)
{
printf("\n elements %d: \t",i+1);
scanf("%d",&ar[i]);
}
printf("\n search for:");
scanf("%d",&v);
for(i=0;i<s;i++)
{
if(ar[i]==v)
{
printf("\n The element is %d in the index %d",v,i+1);
f=1;
break;
}
else
{
f==0;
printf("\n The value does not exist");
}
}
getch();
}