#include<stdio.h>
#include<math.h>
double series(int n)
{
int i;
double sums=0.0,ser;
for(i=1;i<=n;++i)
{
ser=1/pow(i,i);
sums++=ser;
}
return sums;
}
int main()
{
int n;
printf("Enter the limit")
scanf("%d",&n);
double res=series(n);
printf("\n sum of series is %f",res);
return 0;
}

