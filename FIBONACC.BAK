#include<stdio.h>
#include<conio.h>
int fibo(int);
int main()
{
int num,sum;
int results;
printf("Enter the nth number in fibonacci series:");
scanf("%d",&sum);
if(num<0)
{
printf("fibonacci of negative number is not pissible \n");
}
else
{
results=fibo(num);
printf("The %d number in fibonacci series is %d \n",num,results);
}
return 0;
}
int fibo(int num)
{
if(num==0)
{
return 0;
}
else if(num==1)
{
return 1;
}
else
{
return(fibo(num-1)+fibo(num-2));
}
getch();
}
