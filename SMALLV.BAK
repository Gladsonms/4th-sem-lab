#include<stdio.h>
#include<conio.h>
void main()
int a[100];
{
 int i,n,k,j;
 printf("Enter the size of the array:");
 scanf("%d",&n);
 for(i=0;i<n;i++)
 {
  for(k=0;k<n;k++)
  {
   if(a[k+1]<a[k])
   {
    j=a[k];
    a[k]=a[k+1];
    a[k+1
}